<div class="image-display">
  <img src="{{beer.image}}" alt="{{beer.name}}" />
  <h2>{{beer.name}} ${{beer.cost}}</h2>
  <h3>{{beer.style}} / {{beer.brewery}}</h3>
  <button {{action 'addToCart' beer}}>Add to Cart</button>
  {{#link-to 'beers'}}<p>Back to All Beers</p>{{/link-to}}
</div>
