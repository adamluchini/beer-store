import { priceTotal } from 'beer-store/helpers/price-total';
import { module, test } from 'qunit';

module('Unit | Helper | price total');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = priceTotal([42]);
  assert.ok(result);
});
